prefecture
都道府県名。
例：東京都

municipality
調査対象名。
自治体名だけでなく、「東京都都市計画情報」「新宿区道路」のような調査対象名も入れる。

category
そのURLの種類。

urban_plan
都市計画情報

road
道路情報、指定道路図、道路台帳

hazard
ハザード情報

official
自治体公式トップ、または総合案内

district_plan
地区計画

building_guidance
建築指導課、建築確認系

target_url
調査対象URL。
BOTが実際に開いたページ。

status
BOTがそのページを開けたか。

ok
開けた

error
開けなかった、またはタイムアウト

detected_types
BOTが通信ログから検出した種類。

arcgis
ArcGIS系の可能性あり

geojson
GeoJSONデータありの可能性

wms_wfs
WMS/WFS系のGIS APIの可能性

pdf
PDFリンクあり

query_api
裏側でAPI通信している可能性

hazard
ハザード系サイトの可能性

important_urls
BOTが重要そうだと判断した通信URL。
今後、自動取得できるか確認する材料。

error
エラー内容。
statusがerrorのときに原因を見る。


HP
都市計画ナビ β
更新運用レジュメ

━━━━━━━━━━━━━━━━━━
現在の構成
━━━━━━━━━━━━━━━━━━

VS Code
↓
GitHub
↓
Streamlit Cloud
↓
公開URL

現在の公開URLは、
GitHubへ push された内容を自動反映しています。

重要：
VS Codeで保存しただけでは、
公開サイトは更新されません。

必ず：

git push

が必要です。

━━━━━━━━━━━━━━━━━━
更新手順
━━━━━━━━━━━━━━━━━━

STEP1
VS Codeで修正

例：

・app.py 修正
・external_reference_links.csv 更新
・CSS修正
・URL追加
・表示順変更

保存する。

━━━━━━━━━━━━━━━━━━
STEP2
ターミナルを開く
━━━━━━━━━━━━━━━━━━

VS Code 下部：

「ターミナル」

現在位置確認：

```bash
cd C:\Users\uedar\urban_planning_nav
```

━━━━━━━━━━━━━━━━━━
STEP3
変更ファイル追加
━━━━━━━━━━━━━━━━━━

```bash
git add .
```

意味：

変更ファイルを、
GitHubへ送る対象に追加。

━━━━━━━━━━━━━━━━━━
STEP4
変更履歴を保存
━━━━━━━━━━━━━━━━━━

```bash
git commit -m "変更内容"
```

例：

```bash
git commit -m "update hazard urls"
```

例：

```bash
git commit -m "improve ui spacing"
```

意味：

何を変更したか履歴保存。

━━━━━━━━━━━━━━━━━━
STEP5
GitHubへ送信
━━━━━━━━━━━━━━━━━━

```bash
git push
```

意味：

GitHubへ反映。

━━━━━━━━━━━━━━━━━━
STEP6
公開サイトへ自動反映
━━━━━━━━━━━━━━━━━━

通常：

1〜3分程度

で、

```text
https://xxxxx.streamlit.app
```

へ自動反映。

ブラウザ更新で確認。

━━━━━━━━━━━━━━━━━━
重要
━━━━━━━━━━━━━━━━━━

VS Code保存のみ
↓
公開サイト未更新

git push
↓
公開サイト更新

━━━━━━━━━━━━━━━━━━
よく使うコマンド
━━━━━━━━━━━━━━━━━━

更新基本セット：

```bash
git add .
git commit -m "更新内容"
git push
```

━━━━━━━━━━━━━━━━━━
更新例
━━━━━━━━━━━━━━━━━━

CSVへURL追加
↓
保存
↓
ターミナル：

```bash
git add .
git commit -m "add shibuya hazard urls"
git push
```

↓

数分後、
公開サイト反映。

━━━━━━━━━━━━━━━━━━
エラー時
━━━━━━━━━━━━━━━━━━

pushエラー：

```bash
git push
```

を再実行。

━━━━━━━━━━━━━━━━━━
現在の公開構成
━━━━━━━━━━━━━━━━━━

GitHub：
ソース管理

Streamlit Cloud：
公開サイト

VS Code：
編集

━━━━━━━━━━━━━━━━━━
現在の運用方針
━━━━━━━━━━━━━━━━━━

・顧客へURL共有
・CSVを継続育成
・URL未登録を順次削減
・GIS連携は後続開発
・確認先DBを優先育成

━━━━━━━━━━━━━━━━━━
現在の設計思想
━━━━━━━━━━━━━━━━━━

本システムは、

「AIによる断定判定」

ではなく、

「確認先ナビゲーション」

を目的としています。

現在は：

自治体単位
で確認先を整理。

将来的には：

住所
↓
座標変換
↓
GIS判定
↓
確認先表示

へ拡張予定。

